<% if @current_user.id == @activity.host %>
      <%= button_to('我发起的', attends_path(activity_id: @activity), :class =>
          "btn btn-primary btn-pill align-self-center disabled", :style => "color:white;")%>
    <% elsif Attend.find(activity_id: @activity.id, user_id: @current_user.id) != nil %>
      <%= button_to('取消参加', attends_path(activity_id: @activity.id, user_id: @current_user.id), :method => "delete", :class =>
    <% elsif Attend.find_by(activity_id: @activity.id, user_id: @current_user.id) != nil %>
      <%= button_to('取消参加', attend_path(activity_id: @activity.id, user_id: @current_user.id), :method => "delete", :class =>
                "btn btn-primary btn-pill align-self-center", :style => "color:white;")%>
    <% else%>
      <%= button_to('我要参加', attends_path(activity_id: @activity), :class =>

      